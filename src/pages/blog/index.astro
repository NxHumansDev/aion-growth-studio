---
export const prerender = false;

import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { fetchPublishedPosts } from '../../lib/notion';

const lang = 'es';

const sortedPosts = await fetchPublishedPosts('es', 'Economía Digital');
---

<Layout title="Blog — AION Growth Studio" lang={lang}>
  <Header lang={lang} compact />

  <!-- Hero section with brand gradient -->
  <section class="relative overflow-hidden">
    <div class="absolute inset-0 blog-hero-bg"></div>
    <div class="absolute inset-0 blog-hero-dots"></div>
    <div class="relative max-w-7xl mx-auto px-6 md:px-8 py-20 md:py-28">
      <div class="max-w-3xl">
        <p class="text-sm font-medium text-cyan-400 tracking-wide mb-4">AION BLOG</p>
        <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold text-white tracking-tight leading-[0.95] mb-6">
          Inteligencia para<br />crecer
        </h1>
        <p class="text-lg md:text-xl text-slate-300 leading-relaxed max-w-2xl">
          Estrategia, tecnología e IA aplicada al crecimiento de negocios digitales
        </p>
      </div>
    </div>
  </section>

  <main class="py-16 md:py-24 bg-slate-50">
    <div class="max-w-7xl mx-auto px-6 md:px-8">

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {sortedPosts.map((post, i) => (
          <a href={`/blog/${post.slug}`} class="group block">
            <article class="relative bg-white rounded-2xl overflow-hidden border border-slate-200/60 transition-all duration-500 hover:-translate-y-2 hover:shadow-2xl hover:border-aion-primary/20 h-full flex flex-col">
              {post.image && (
                <div class="aspect-[16/9] overflow-hidden relative">
                  <img
                    src={post.image}
                    alt={post.title}
                    class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"
                  />
                  <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
                </div>
              )}

              <div class="p-6 md:p-8 flex flex-col flex-1">
                {post.category && (
                  <span class="inline-block self-start px-3 py-1 bg-aion-dark/5 text-aion-primary text-xs font-semibold rounded-full mb-4 tracking-wide uppercase">
                    {post.category}
                  </span>
                )}

                <h2 class="text-xl font-bold text-slate-900 mb-3 group-hover:text-aion-primary transition-colors duration-300 leading-snug">
                  {post.title}
                </h2>

                <p class="text-slate-500 text-sm mb-6 line-clamp-3 flex-1">
                  {post.description}
                </p>

                <div class="flex items-center justify-between text-xs text-slate-400 pt-4 border-t border-slate-100">
                  <div class="flex items-center gap-2">
                    <div class="w-6 h-6 rounded-full bg-gradient-to-br from-aion-dark to-aion-primary flex items-center justify-center">
                      <span class="text-white text-[10px] font-bold">{post.author?.charAt(0) || 'A'}</span>
                    </div>
                    <span class="font-medium text-slate-600">{post.author}</span>
                  </div>
                  <div class="flex items-center gap-3">
                    <time datetime={post.date}>
                      {new Date(post.date).toLocaleDateString('es-ES', {
                        year: 'numeric',
                        month: 'short',
                        day: 'numeric'
                      })}
                    </time>
                    {post.readTime && (
                      <>
                        <span class="text-slate-300">|</span>
                        <span>{post.readTime}</span>
                      </>
                    )}
                  </div>
                </div>
              </div>
            </article>
          </a>
        ))}
      </div>

      {sortedPosts.length === 0 && (
        <div class="text-center py-20">
          <div class="w-16 h-16 mx-auto mb-6 rounded-2xl bg-aion-dark/5 flex items-center justify-center">
            <svg class="w-8 h-8 text-aion-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19.5 14.25v-2.625a3.375 3.375 0 00-3.375-3.375h-1.5A1.125 1.125 0 0113.5 7.125v-1.5a3.375 3.375 0 00-3.375-3.375H8.25m0 12.75h7.5m-7.5 3H12M10.5 2.25H5.625c-.621 0-1.125.504-1.125 1.125v17.25c0 .621.504 1.125 1.125 1.125h12.75c.621 0 1.125-.504 1.125-1.125V11.25a9 9 0 00-9-9z" />
            </svg>
          </div>
          <p class="text-slate-500 text-lg">Pronto publicaremos nuevos artículos.</p>
        </div>
      )}

    </div>
  </main>

  <Footer lang={lang} />
</Layout>

<style>
  .blog-hero-bg {
    background: linear-gradient(160deg, #0B1B3D 0%, #1A5490 50%, #0B1B3D 100%);
  }
  .blog-hero-dots {
    background-image: radial-gradient(rgba(255, 255, 255, 0.04) 1px, transparent 1px);
    background-size: 32px 32px;
  }
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
