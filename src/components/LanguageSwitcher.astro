---
import { languages } from '../i18n/ui';
import type { Lang } from '../i18n/utils';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const otherLang: Lang = lang === 'es' ? 'en' : 'es';
const otherPath = `/${otherLang}/`;
---

<a
  href={otherPath}
  class="inline-flex items-center gap-1.5 px-3 py-1.5 text-sm font-medium text-on-surface/60 hover:text-on-surface rounded-md hover:bg-surface-container-low transition-colors"
  aria-label={`Switch to ${languages[otherLang]}`}
>
  <span class="uppercase tracking-wide text-xs">{otherLang}</span>
</a>
